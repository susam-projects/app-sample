import { createStyles } from "antd-style";

export const useStyles = createStyles(({ css }) => ({
  wrapper: css`
    display: flex;

    align-items: center;
    justify-content: center;

    height: 100vh;
  `,
}));
