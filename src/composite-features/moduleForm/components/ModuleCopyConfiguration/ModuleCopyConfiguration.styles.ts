import { createStyles } from "antd-style";

export const useStyles = createStyles(({ css }) => ({
  typographyHeader: css`
    margin-bottom: 35px;

    font-size: large;
    font-weight: bold;
  `,
  rowMarginTop: css`
    margin-top: 120px;
  `,
}));
